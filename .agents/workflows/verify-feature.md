---
description: Verify a local Ariadna feature in the browser
---

When the user types `/verify-feature <scenario>`, do this:

1. Act as @qa.
2. Use the `browser-check` skill.
3. Open the local app if needed.
4. Run the requested scenario.
5. Report:
   - what works
   - what breaks
   - reproduction steps
   - smallest likely cause if obvious